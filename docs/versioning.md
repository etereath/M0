# 版本规范

公共契约采用语义化版本，`VERSION` 保存不带 `v` 前缀的当前版本，Git Tag 和 Release 使用 `v<版本>`。

- PATCH：文档、样例、链接、路径或不改变契约含义的修正；
- MINOR：新增可选字段、向后兼容的 Schema、接口或枚举值（并定义旧客户端的默认处理）；
- MAJOR：删除或改名字段、改变字段或枚举含义、改变状态机或其他破坏兼容性的修改。

在 1.0 之前仍须记录每项变更、维护 fixtures，并通过全部契约校验。业务项目应固定依赖 Release、Tag 或 commit，而非无条件跟随 `main`。
